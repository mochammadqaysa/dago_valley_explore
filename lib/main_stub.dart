/// Stub for web platform — desktop window initialization is not available.

Future<void> initDesktopWindow() async {
  // No-op on web
}

void logError(String msg) {
  // No-op on web; errors are printed to console in main.dart
}
